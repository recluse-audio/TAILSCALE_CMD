#+TITLE: CMD
#+REPO: TAILSCALE_CMD

* set pi host
#+NAME: Set-Pi-Host
#+KIND: reference
Edit ~pi_host.conf~ at this repo's root, one line: ~PI_HOST=user@tailnet-hostname~.
Find the tailnet hostname with ~tailscale status~ once the pi has run
~RD_CMD/CONFIG/COMMON/TAILSCALE/setup.sh~ and been authorized in the browser.

* connect to tailnet
#+NAME: Connect
#+KIND: manual
#+PLAIN: Joins this computer to the tailnet and lets peers SSH in, key-free.
#+DESC: runs tailscale up --ssh, then lists every device on the tailnet
#+OUTPUT: no files, connects the network interface and prints a login URL on first run
#+NEXT: run List-Cameras once the pi shows up in the peer list
#+BEGIN_SRC sh
sudo tailscale up --ssh
tailscale status
#+END_SRC

#+RESULTS:
|  100.108.130.75 | artie-thinkpad-p51s-w10dg | ryandevens92@ | linux   | -        |      |      |      |      |
|   100.120.14.55 | artiepc                   | ryandevens92@ | windows | -        |      |      |      |      |
|    100.89.27.73 | pi-21c865                 | ryandevens92@ | linux   | offline, | last | seen | 179d | ago  |
| 100.114.174.118 | raspi-calvin              | ryandevens92@ | linux   | offline, | last | seen |  99d | ago  |
|   100.95.183.40 | raspi-desktop             | ryandevens92@ | linux   | -        |      |      |      |      |
|   100.101.162.4 | raspi-harrison            | ryandevens92@ | linux   | offline, | last | seen | 100d | ago  |
|   100.96.47.108 | raspi-organizer           | ryandevens92@ | linux   | offline, | last | seen |  70d | ago  |
| 100.115.108.109 | raspi-parishilton         | ryandevens92@ | linux   | idle,    | tx   | 5308 |   rx | 5732 |

* ssh test ping
#+NAME: SSH_Test_Ping
#+KIND: ui_script
#+PLAIN: Asks which online tailnet device to target, then checks ping and SSH into it.
#+DESC: lists online peers, runs tailscale ping, then tailscale ssh user@host and prints hostname
#+BEFORE: add the device's login user to ssh_users.conf, or type it at the prompt
#+OUTPUT: prints to this terminal, writes nothing
#+BEGIN_SRC sh :session
./SCRIPTS/ssh_test_ping.sh
#+END_SRC

#+RESULTS:

* list cameras
#+NAME: List-Cameras
#+KIND: ui_script
#+PLAIN: Shows which of the pi's four USB webcams are present, as /dev/videoN.
#+DESC: SSHes into the pi over the tailnet and lists /dev/video* plus v4l2-ctl detail
#+BEFORE: set PI_HOST in pi_host.conf first
#+OUTPUT: prints to this terminal, writes nothing
#+NEXT: run View-Camera and pick a webcam from its menu
#+BEGIN_SRC sh
./SCRIPTS/list_cameras.sh
#+END_SRC

* view camera
#+NAME: View-Camera
#+KIND: ui_script
#+GROUP: cameras
#+PLAIN: Asks which USB webcam plugged into the pi to show, then opens it in a local window.
#+DESC: lists /dev/v4l/by-id capture nodes on the pi, streams the pick as mjpeg into ffplay
#+BEFORE: set PI_HOST in pi_host.conf first
#+OUTPUT: opens an ffplay window, writes nothing to disk
#+BEGIN_SRC sh :session
./SCRIPTS/view_camera.sh
#+END_SRC
