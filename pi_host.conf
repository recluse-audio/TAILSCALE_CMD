# Tailnet target for the webcam pi. Edit this once the pi is on the tailnet.
# Format: user@tailnet-hostname (see `tailscale status` for the hostname).
# pi-organizer is where a single webcam ran before (see
# NOTES/DEV_KNOWLEDGE/GENERAL/PI_TailScale.md in the NOTES repo); change this
# if the 4-webcam setup lands on a different or newly provisioned pi.
PI_HOST=parishilton@raspi-parishilton
